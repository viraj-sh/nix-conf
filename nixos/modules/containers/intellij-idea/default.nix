# Auto-generated by compose2nix.

{ pkgs, lib, ... }:

{
  # Runtime
  virtualisation.docker = {
    enable = true;
    autoPrune.enable = true;
  };
  virtualisation.oci-containers.backend = "docker";

  # Containers
  virtualisation.oci-containers.containers."intellij-idea" = {
    image = "lscr.io/linuxserver/intellij-idea:latest";
    environment = {
      "PGID" = "1000";
      "PUID" = "1000";
      "TZ" = "Asia/Kolkata";
    };
    volumes = [
      "/home/virajs-server/docker/intellij-idea/data/config:/config:rw"
    ];
    log-driver = "journald";
    extraOptions = [
      "--network-alias=intellij-idea"
      "--network=nginx"
      "--shm-size=1073741824"
    ];
  };
  systemd.services."docker-intellij-idea" = {
    serviceConfig = {
      Restart = lib.mkOverride 90 "no";
    };
    partOf = [
      "docker-compose-intellij-idea-root.target"
    ];
    wantedBy = [
      "docker-compose-intellij-idea-root.target"
    ];
  };

  # Root service
  # When started, this will automatically create all resources and start
  # the containers. When stopped, this will teardown all resources.
  systemd.targets."docker-compose-intellij-idea-root" = {
    unitConfig = {
      Description = "Root target generated by compose2nix.";
    };
    wantedBy = [ "multi-user.target" ];
  };
}
