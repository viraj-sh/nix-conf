# ntfy Setup Instructions

## 1. Overview
[ntfy](https://docs.ntfy.sh/install/#docker) lets you send push notifications to your phone or desktop via scripts from any computer, using simple HTTP PUT or POST requests. I use it to notify myself when scripts fail, or long-running commands complete.

## 2. Prerequisites
- Default port: `80`
