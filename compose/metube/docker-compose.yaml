name: metube
services:
  metube:
    image: ghcr.io/alexta69/metube
    container_name: metube
    restart: unless-stopped
    # ports:
    #   - "${METUBE_PORT}:8081"
    volumes:
      - /home/virajs-server/docker/metube/data/downloads:/downloads
    networks:
      - nginx
networks:
  nginx:
    external: true
